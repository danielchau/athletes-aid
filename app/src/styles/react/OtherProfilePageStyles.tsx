import { makeStyles, Theme, createStyles } from "@material-ui/core/styles";

/**
 * React styles for the Other Profile Page container which holds a profile page.
 */
export const otherProfilePageStyles = makeStyles((theme: Theme) =>
    createStyles({
        root: {
            display: "flex",
            width: "100%"
        }
    })
);
